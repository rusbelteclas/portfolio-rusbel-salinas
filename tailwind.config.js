module.exports = {
    darkMode: 'class', // Habilita el soporte para modo oscuro con la clase "dark"
    content: ['./src/**/*.{js,jsx,ts,tsx}'], // Rutas de tus archivos
    theme: {
        extend: {
        },
    },
    plugins: [],
};
